import 'package:flutter/material.dart';
//import '../models/camera/main_camera_model.dart';

class StartCamera {
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [Text('je suis la')],
      ),
    );
  }
}
